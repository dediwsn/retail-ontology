# ----- Existing -----
.superpowers/
node_modules/
cdk.out/
.env*
*.pyc
__pycache__/
.DS_Store

# ----- Python -----
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ----- Next.js -----
.next/
out/
.turbo/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----- AWS CDK (additional) -----
infra-cdk/cdk.context.json
infra-cdk/.cdk.staging/

# ----- Claude Code -----
# Personal allow list — 240+ entries per developer.
# Project-shared deny + hooks live in .claude/settings.json (tracked).
.claude/settings.local.json

# ----- Build / coverage -----
*.pid
*.seed
*.pid.lock
.coverage
htmlcov/
dist/
build/

# ----- Synthetic data outputs (regenerated by data/load.py) -----
data/output/*.json
data/output/*.ndjson

# ----- Editor / OS -----
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.swn

# ----- Local-only -----
/tmp/
*.tmp

/.local/

# graphify-out/ is regenerable via `graphify update . --force` — only the
# bundled snapshot copied into web/public/codegraph/ is shipped.
/graphify-out/
